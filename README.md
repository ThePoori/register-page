# Register Page

An account registration and verification module built with Django, part of the `eshop_project` e-commerce application.

## 🚀 Features

- User registration form with validation
- Account verification flow (e.g. activation/confirmation step)
- Django-powered backend with HTML/CSS frontend

## 🛠️ Tech Stack

- **Backend:** Python / Django
- **Frontend:** HTML, CSS

## 📦 Project Structure

```
register-page/
├── eshop_project/   # Django project files (registration & account verification)
├── .idea/           # IDE config (PyCharm)
└── README.md
```

## ⚙️ Setup & Run

1. Clone the repository
   ```bash
   git clone https://github.com/ThePoori/register-page.git
   cd register-page/eshop_project
   ```

2. Create a virtual environment and install dependencies
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   pip install django
   ```

3. Run migrations
   ```bash
   python manage.py migrate
   ```

4. Start the development server
   ```bash
   python manage.py runserver
   ```

5. Open your browser at `http://127.0.0.1:8000/`

## 👥 Contributors

- [ThePoori](https://github.com/ThePoori) — Backend Developer
- [MohamadYasiN](https://github.com/Prophet013) — Frontend Developer

## 📄 License

This project currently has no license specified.

---

<div dir="rtl">

# صفحه ثبت‌نام

ماژول ثبت‌نام و تایید حساب کاربری، ساخته‌شده با جنگو، بخشی از پروژه‌ی فروشگاهی `eshop_project`.

## 🚀 امکانات

- فرم ثبت‌نام کاربر همراه با اعتبارسنجی (validation)
- فرآیند تایید حساب (مثلاً مرحله‌ی فعال‌سازی/تایید)
- بک‌اند جنگو همراه با فرانت‌اند HTML/CSS

## 🛠️ تکنولوژی‌های استفاده شده

- **بک‌اند:** Python / Django
- **فرانت‌اند:** HTML, CSS

## 📦 ساختار پروژه

```
register-page/
├── eshop_project/   # فایل‌های پروژه‌ی جنگو (ثبت‌نام و تایید حساب)
├── .idea/           # تنظیمات IDE (PyCharm)
└── README.md
```

## ⚙️ نصب و اجرا

۱. کلون کردن ریپازیتوری
   ```bash
   git clone https://github.com/ThePoori/register-page.git
   cd register-page/eshop_project
   ```

۲. ساخت محیط مجازی و نصب پکیج‌ها
   ```bash
   python -m venv venv
   source venv/bin/activate  # در ویندوز: venv\Scripts\activate
   pip install django
   ```

۳. اجرای مایگریشن‌ها
   ```bash
   python manage.py migrate
   ```

۴. اجرای سرور توسعه
   ```bash
   python manage.py runserver
   ```

۵. باز کردن مرورگر روی آدرس `http://127.0.0.1:8000/`

## 👥 همکاران پروژه

- [ThePoori](https://github.com/ThePoori) — توسعه‌دهنده بک‌اند
- [MohamadYasiN](https://github.com/Prophet013) — توسعه‌دهنده فرانت‌اند

## 📄 لایسنس

در حال حاضر این پروژه لایسنس خاصی ندارد.

</div>
